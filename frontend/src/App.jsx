import AppRoutes from "./routes/AppRoutes";

/**
 * Root component of the application.
 * Renders the application's routing configuration.
 *
 * @component
 * @returns {JSX.Element}
 */
function App() {
  return <AppRoutes />;
}

export default App;